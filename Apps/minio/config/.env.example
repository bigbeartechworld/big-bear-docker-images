# MinIO Environment Variables
# Copy this file to .env and modify as needed

# Root credentials - REQUIRED
# Change these values in production!
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Optional: Custom user/group settings
# Uncomment and modify if you need to run MinIO as a specific user
# MINIO_USERNAME=minio
# MINIO_GROUPNAME=minio
# MINIO_UID=1000
# MINIO_GID=1000

# Optional: Server configuration
# MINIO_REGION=us-east-1
# MINIO_BROWSER=on
# MINIO_DOMAIN=minio.example.com

# Optional: Logging
# MINIO_LOG_LEVEL=INFO

# Optional: Notification endpoints
# MINIO_NOTIFY_WEBHOOK_ENABLE=on
# MINIO_NOTIFY_WEBHOOK_ENDPOINT=http://example.com/webhook
